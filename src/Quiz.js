import QuizQuestion from './QuizQuestion.js';

export default function Quiz({ props }) {
    let questions = [];
    
    return (
        // Number of questions -- numberOfQuestions
        // Operations -- operations
        // Minimum and Maximum Bound -- minBound, maxBound

        props.
    );
}